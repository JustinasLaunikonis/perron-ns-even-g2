## What does this PR do?

<!-- A clear, short description of the change. -->

## Why?

<!-- The motivation or the problem this solves. Link any related issue, e.g. Closes #123 -->

## How was it tested?

<!-- e.g. ran `npm run build`, checked the change in the simulator (`npm run dev` + `npm run simulate`) -->

## Checklist

- [ ] `npm run build` passes (type-check + bundle, no errors)
- [ ] No code comments added (project is comment-free)
- [ ] User-facing text goes through `src/i18n.ts` (`t('key')`), not hard-coded
- [ ] No secrets or API keys added to the app or `app.json`
- [ ] This PR is one focused, logical change
